#ifndef PLAYERFLOPPY_H
#define PLAYERFLOPPY_H

#include <vector>
#include <initializer_list>
#include "Floppy.h"

class FloppyPlayer
{
public:
    FloppyPlayer( std::initializer_list<int> pins );
    Play( std::vector< std::vector<note>* > );
private:
    std::vector<Floppy*> floppies;        
}


#endif
